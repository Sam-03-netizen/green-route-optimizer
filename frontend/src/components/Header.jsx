function Header() {
  return (
    <div className="hero">
      <h1>🚛 Green Route OpenEnv Dashboard</h1>
      <p className="tagline">
        A benchmark environment for carbon-aware fleet planning, route execution,
        and delivery optimization using the OpenEnv step/reset/state API.
      </p>
      <div className="status-badge">🟢 OpenEnv Backend Live</div>
    </div>
  );
}

export default Header;